annotated-types==0.7.0
anyio==4.3.0  # Cần tự tìm phiên bản anyio bạn muốn dùng. Ví dụ: 4.3.0
asttokens # Không cần phiên bản nếu không có vấn đề tương thích. Hoặc pin phiên bản nếu bạn biết.
certifi # Tương tự asttokens
click==8.2.1
cloudpickle==3.1.1
colorama # Tương tự asttokens
comm # Tương tự asttokens
contourpy==1.3.2
cycler==0.12.1
debugpy # Nếu không dùng trong production, hãy xóa dòng này
decorator # Tương tự asttokens
dotenv==0.9.9
exceptiongroup # Tương tự asttokens
executing # Tương tự asttokens
fastapi==0.116.0
fonttools==4.58.5
h11 # Tương tự asttokens
h2 # Tương tự asttokens
hpack # Tương tự asttokens
httpcore # Tương tự asttokens
httpx # Tương tự asttokens
hyperframe # Tương tự asttokens
idna # Tương tự asttokens
imageio==2.37.0
importlib_metadata # Tương tự asttokens
ipykernel # Nếu không dùng trong production, hãy xóa dòng này (thường dùng cho Jupyter)
ipython # Nếu không dùng trong production, hãy xóa dòng này (thường dùng cho Jupyter)
jedi # Tương tự asttokens
joblib==1.5.1
jupyter_client # Nếu không dùng trong production, hãy xóa dòng này
jupyter_core # Nếu không dùng trong production, hãy xóa dòng này
kiwisolver==1.4.8
lazy_loader==0.4
lime==0.2.0.1
llvmlite==0.44.0
matplotlib==3.10.3
matplotlib-inline # Tương tự asttokens
nest_asyncio # Tương tự asttokens
networkx==3.4.2
numba==0.61.2
numpy==2.2.6
packaging # Tương tự asttokens
pandas==2.3.1
parso # Tương tự asttokens
pickleshare # Tương tự asttokens
pillow==11.3.0
platformdirs # Tương tự asttokens
prompt_toolkit # Tương tự asttokens
psutil # Tương tự asttokens
pure_eval # Tương tự asttokens
pydantic==2.11.7
pydantic_core==2.33.2
Pygments # Tương tự asttokens
pyparsing==3.2.3
python-dateutil # Tương tự asttokens
python-dotenv==1.1.1
pytz==2025.2
pywin32==307 # Cần xem xét, pywin32 là cho Windows, Render chạy Linux. Nếu API của bạn không phụ thuộc vào nó, hãy xóa.
pyzmq # Tương tự asttokens. Nếu không dùng trong production, hãy xóa dòng này.
scikit-image==0.25.2
scikit-learn==1.7.0
scipy==1.15.3
seaborn==0.13.2
shap==0.48.0
six # Tương tự asttokens
slicer==0.0.8
sniffio # Tương tự asttokens
stack_data # Tương tự asttokens
starlette==0.46.2
threadpoolctl==3.6.0
tifffile==2025.5.10
tornado # Tương tự asttokens
tqdm==4.67.1
traitlets # Tương tự asttokens
typing-inspection==0.4.1
typing_extensions # Tương tự asttokens
tzdata==2025.2
uvicorn==0.35.0
wcwidth # Tương tự asttokens
zipp # Tương tự asttokens
